apiVersion: v1
kind: Service
metadata:
  name: api-banners-service
  namespace: api 
spec:
  type: NodePort
  selector:
    app: api-banners
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30084
  
